const INVALID_TOKEN = 'INVALID_TOKEN';

const RESPONSE_STATUS = {
  SUCESS: 200,
  NOT_FOUND: 404,
  INTERVAL_SERVER: 500,
  FORBIDDEN: 403,
};

export default {
  INVALID_TOKEN,
  RESPONSE_STATUS,
};
